Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. 

Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. 

Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. Replica-per-GPU serving trades memory duplication for zero inter-device transfers. Splitting transformer layers across GPUs moves activations over PCIe or P2P links. Wavefront size shapes how matrix cores pack work on RDNA4 compute units. A paged KV allocator avoids fragmentation by carving blocks of fixed token capacity. Speculative drafting spends small-model forwards to skip large-model decode steps. Bandwidth-bound decode rewards batching more than compute-bound prefill does. Thermal throttling can silently erase measured gains between benchmark legs. Controller placement needs honest per-card capability numbers to beat sticky rank zero. Prefill latency dominates time-to-first-token for interactive inference. KV cache growth is linear in context length and quadratic attention cost follows it. 
